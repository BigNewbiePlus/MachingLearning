#include<vector>
using namespace std;
class Person{
public:
    int age;
};

int main()
{
    int b;
    Person lily;
    vector<int> a;

    for(b=lily.age;b<lily.age;b++){
    }
    for(int i=lily.age;i<lily.age;i++){
    }

    for(vector<int>::iterator it=a.begin(); it!=a.begin();it++){
    }

    vector<int>::iterator it;

    for(it=a.begin(); it!=a.begin();it++){
    }
    return 0;
}
