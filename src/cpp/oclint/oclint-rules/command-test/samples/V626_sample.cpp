#include<vector>
int main()
{
    int a;
    if(a)
        a=1;
    else
        a=2,
    a=3;

    int* ptr;
    if(ptr==0)
        delete ptr,
    ptr=0;

    if(ptr==0)
        return a,
    a=0;

    return 0;
}
