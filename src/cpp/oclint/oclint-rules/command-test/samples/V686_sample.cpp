int main()
{
    bool tipsel, rootsel;
    if ((tipsel && rootsel) || (rootsel)) {}
    
    bool tm_mday, cfg_start_day, before;
    if (tm_mday < cfg_start_day ||
        (tm_mday < cfg_start_day && before)) {
      ;
    }
    char nextc;
    if (nextc == '*' || (tipsel && nextc == '*')){}
    int vibration, sound, sound_type;
    if(((vibration == 1 && sound == 0) || sound == 0) &&
        sound_type == 1) {
        ;
    }
    return 0;
}
